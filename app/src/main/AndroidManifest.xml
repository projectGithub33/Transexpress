<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc.transexpress">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Transaction" />
        <activity
            android:name=".AdminAcceuilActivity"
            android:label="@string/title_activity_admin_acceuil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminSuiviServiceActivity">

        </activity>
        <activity android:name=".CientEtatTransfertActivity" />
        <activity android:name=".ClientValideeArriveeCommandeActivity" />
        <activity android:name=".ClientArchiveTransfertActivity" />
        <activity android:name=".ClientEffectuerServiceActivity" />
        <activity android:name=".InfoActivity" />
        <activity
            android:name=".ClientAcceuilActivity"
            android:label="@string/title_activity_client_acceuil"
            android:theme="@style/AppTheme.NoActionBar" >
   
        </activity>
        <activity android:name=".SignActivity"></activity>
    </application>

</manifest>
